class Solution {
public:
    int missingNumber(vector<int>& n) {
        vector<int> a(n.size()+1);
        for(int i:n){
            a[i]++;
        }
        for(int i=0;i<=n.size();i++){
            if(a[i]==0) return i;
        }
        return -1;
    }
};
